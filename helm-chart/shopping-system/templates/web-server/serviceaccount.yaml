{{- if .Values.webServer.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "shopping-system.web-server.serviceAccountName" . }}
  labels:
    {{- include "shopping-system.labels" . | nindent 4 }}
  {{- with .Values.webServer.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.webServer.serviceAccount.automount }}
{{- end }}
