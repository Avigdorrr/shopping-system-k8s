apiVersion: v1
kind: Service
metadata:
  name: {{ include "management-api.fullname" . }}
  labels:
    {{- include "management-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "management-api.selectorLabels" . | nindent 4 }}
